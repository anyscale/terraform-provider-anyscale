```
release-note:fixed
resource/anyscale_organization_user: The example now correctly describes applying this resource for a non-member as sending them an invitation, which they must still accept before they have access.
```
```
release-note:fixed
provider: README.md now correctly describes anyscale_organization_user (invites a non-member; no longer manages role) and lists anyscale_organization_user_role, previously missing entirely.
```
```
release-note:fixed
provider: anyscale_organization_user, anyscale_organization_user_role, anyscale_organization_invitation, both organization_user data sources, and the RBAC guide now cross-link to each other, and the provider's landing page gains a guide index.
```
```
release-note:fixed
provider: The RBAC guide and the organization_user_workflow example now describe a locals map plus for_each as the idiomatic way to manage a team's membership and roles together, not a workaround; the example uses one map as the single source of truth for both the invitation and role loops.
```
